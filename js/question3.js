function chunkIt(arr, length) {
    var newChunk = [];
    
    if(var i = 0; i < arr.length; i++){
        
      newChunk.push(arr.slice(i + length))
  
    }
  
  }
  
  chunkIt(["potatoes","molasses","greg","wirt"], 2) 